// lib/features/capsules/screens/add_collaborators_screen.dart

import 'package:flutter/material.dart';

class AddCollaboratorsScreen extends StatelessWidget {
  const AddCollaboratorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Collaborators'),
      ),
      body: const Center(
        child: Text('Collaborator Selection UI Goes Here (Screen 15)'),
      ),
    );
  }
}